# Review Render's current cost estimate before provisioning paid resources.
services:
  - type: web
    name: personaai-api
    runtime: docker
    plan: 0.5c-512mb
    region: frankfurt
    dockerCommand: python -m app.cloud web
    healthCheckPath: /health/ready
    autoDeployTrigger: "off"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: personaai-db
          property: connectionString
      - key: APP_URL
        sync: false
      - key: ENVIRONMENT
        value: production
      - key: COOKIE_SECURE
        value: "true"
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_WEBHOOK_SECRET
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_MODEL
        value: gpt-4.1-mini
  - type: worker
    name: personaai-worker
    runtime: docker
    plan: 0.5c-512mb
    region: frankfurt
    dockerCommand: python -m app.cloud worker
    autoDeployTrigger: "off"
    envVars: &shared
      - key: DATABASE_URL
        fromDatabase:
          name: personaai-db
          property: connectionString
      - key: ENVIRONMENT
        value: production
      - key: COOKIE_SECURE
        value: "true"
      - key: APP_URL
        fromService:
          type: web
          name: personaai-api
          envVarKey: APP_URL
      - key: TELEGRAM_BOT_TOKEN
        fromService:
          type: web
          name: personaai-api
          envVarKey: TELEGRAM_BOT_TOKEN
      - key: TELEGRAM_WEBHOOK_SECRET
        fromService:
          type: web
          name: personaai-api
          envVarKey: TELEGRAM_WEBHOOK_SECRET
      - key: OPENAI_API_KEY
        fromService:
          type: web
          name: personaai-api
          envVarKey: OPENAI_API_KEY
      - key: OPENAI_MODEL
        fromService:
          type: web
          name: personaai-api
          envVarKey: OPENAI_MODEL
  - type: cron
    name: personaai-retention
    runtime: docker
    plan: 0.5c-512mb
    region: frankfurt
    schedule: "17 * * * *"
    dockerCommand: python -m app.cloud purge
    autoDeployTrigger: "off"
    envVars: *shared

databases:
  - name: personaai-db
    plan: 0.1c-256mb
    region: frankfurt
    postgresMajorVersion: "16"
    databaseName: personaai
    user: personaai
    diskSizeGB: 5
    ipAllowList: []
